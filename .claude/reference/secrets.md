# Secrets & environment variables

> Env var names, what they key, where consumed. Never store secret VALUES here.

**None.** Offline app — no cloud, no accounts, no API keys, no network at runtime (one-time model download excepted). If that ever changes, record names + purposes here.
